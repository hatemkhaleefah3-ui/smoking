Read the entire attached lecture file and convert it into a new JSON file that follows exactly the same structure, field names, nesting, and supported block types as the attached example JSON file.

Requirements:

1. Read the entire lecture file from beginning to end.
2. Review every page and all available content.
3. The lecture file may contain text, tables, images, diagrams, charts, screenshots, figures, and captions.
4. Read and use relevant information contained in images and other visual elements.
5. Use captions and surrounding text to understand visual content.
6. Do not ignore important information because it appears inside an image, chart, diagram, screenshot, or table.
7. Do not guess unclear visual details. Omit uncertain information instead of inventing it.
8. Use only information found in the lecture file.
9. Do not copy lecture content from the example JSON file. Use it only as the required output structure.
10. Preserve the lecture's original language.
11. Keep important information while removing obvious repetition, filler, and accidental duplication.
12. Organize the content into clear and logical sections.
13. Include every field required by the example JSON file.
14. If optional information is missing, use the same empty-value style shown by the example.
15. Do not add unsupported facts, examples, citations, names, dates, or conclusions.
16. Do not include HTML or Markdown inside JSON values.
17. Make every section ID unique, lowercase, and kebab-case.
18. Ensure all table rows have the same number of cells as their headers.
19. Ensure the completed result is valid JSON with no trailing commas.
20. Create the final result as a downloadable .json file with a clear filename based on the lecture title.
21. Do not provide explanations, Markdown code fences, or extra text instead of the file.

Before creating the file, verify that the entire lecture was reviewed, relevant visual content was considered, the result follows the example JSON exactly, the JSON parses correctly, and no information was invented.
